[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn gemini_code:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/google724c2c35ebc454fc.html"
healthcheckTimeout = 100
